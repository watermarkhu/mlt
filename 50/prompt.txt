Rule IDs: RESWD, SYNEND, MCPLD, BADNOT, BADNOTLHS
Category: Syntax Errors (3.7)
Module: crates/mlt_rules/src/syntax_errors.rs

You are the Reviewer in mlt's rule pipeline. Review the implementation plan for the SYNTAX G3 group (reserved words & invalid identifier contexts).

READ the plan at /tmp/opencode/plans/SYNTAX_G3.md first.

Then verify against the codebase:
- Read crates/mlt_rules/src/syntax_errors.rs (especially walk_error_nodes, looks_like_missing_bracket, looks_like_missing_end, check_file dispatch, tests mod).
- Run the AST probe in /tmp/opencode/astprobe to confirm how `else\n` standalone, `end = 5;`, `y = end;`, `x = ~ = 5;`, `~ = 5;`, and `[x ~ y] = f();` parse.

KEY VERIFIED FINDINGS already in the plan: `~ = 5;` parses as a VALID assignment with `ignored_argument` LHS (no ERROR); `x = ~ = 5;` is a `not_operator` wrapping an ERROR; `[~, y]`/`[~]` are valid. Adjudicate: is the BADNOTLHS detection (multioutput `~` adjacent to outputs without comma, e.g. `[x ~ y]`) correct? Is MCPLD's properties-context detection right? RESWD vs SYNEND split correct? Are MUST/MUST NOT cases right?

Return exactly:
- `VERDICT: APPROVED` or `VERDICT: CHANGES REQUIRED`
- A short list of required corrections (or confirmations if approved)
- The corrected, complete implementation plan (repeat the whole plan with fixes) — self-contained, since the Implementer only sees your message.