Rule IDs: UNSET, LHROW
Category: Syntax Errors (3.7)
Module: crates/mlt_rules/src/syntax_errors.rs

You are the Reviewer in mlt's rule pipeline. Review the implementation plan for the SYNTAX G4 group (assignment LHS problems).

READ the plan at /tmp/opencode/plans/SYNTAX_G4.md first.

Then verify against the codebase:
- Read crates/mlt_rules/src/syntax_errors.rs (especially check_statements, NOLHS handling, check_file dispatch, tests mod).
- Run the AST probe in /tmp/opencode/astprobe to confirm how `x == 5 = 3;`, `x + 1 = 2;`, `[a;b] = f();`, and `[a, b] = f();` parse.

Adjudicate: is UNSET's detection (assignment `left` field is an operator node) implementable with the real grammar? Is LHROW's `;` in multioutput_variable detection right? Are MUST/MUST NOT cases right? file/step/test requirements complete?

Return exactly:
- `VERDICT: APPROVED` or `VERDICT: CHANGES REQUIRED`
- A short list of required corrections (or confirmations if approved)
- The corrected, complete implementation plan (repeat the whole plan with fixes) — self-contained, since the Implementer only sees your message.