You are splitting the `performance` rule engine in `crates/mlt_rules/src/performance.rs` into a directory of one-file-per-check modules. PURE MECHANICAL REFACTOR — zero behavior change.

## CRITICAL CONSTRAINTS
- Do NOT delete or modify `crates/mlt_rules/src/performance.rs`.
- Do NOT modify `crates/mlt_rules/src/lib.rs`.
- Only CREATE new files under `crates/mlt_rules/src/performance/`.
- Do NOT commit.

## Target layout
```
crates/mlt_rules/src/performance/
├── mod.rs                        # PerformanceEngine, config, impl Rule (dispatch), inventory::submit!,
│                                #   shared free helpers (pub(crate)), module doc, shared #[cfg(test)] helpers
├── check_*.rs                    # one file per independent check
└── ...
```

## How to do it
1. Read `crates/mlt_rules/src/performance.rs` fully (it has ~9 check_ methods, 81 tests).
2. `mod.rs` gets: module `//!` doc, `use` statements, config struct, `PerformanceEngine` struct, ALL free helpers + consts (TARGET_NODES, lookup tables), the `impl Rule` with `check()`/`check_file()` dispatch, and `inventory::submit!`. Make shared free helpers `pub(crate)`.
3. Each INDEPENDENT `check_*` method → its own file `performance/check_<name>.rs`: `use super::*; impl PerformanceEngine { pub(crate) fn check_<name> ... }` + that check's tests.
4. Methods sharing MUTABLE traversal state (e.g. a file-level growth-analysis walk with accumulated state) stay in mod.rs.
5. Move tests with their check; generic engine tests stay in mod.rs.

## Verification (REQUIRED)
- Diff each moved method body against the original (identical except `pub(crate)` + indentation).
- Confirm each check ID appears exactly once across `performance/`.
- Confirm dispatch references match; moved-test count matches.
You may write a diff script; do NOT run `cargo build`.

## Report back
- Files created
- Moved vs kept methods
- Verification results
- Risks/notes

Do not touch anything outside `crates/mlt_rules/src/performance/`.