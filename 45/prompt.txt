Rule ID: VTPOD
Category: Syntax Errors (3.7)
Module: crates/mlt_rules/src/syntax_errors.rs

You are the Reviewer in mlt's rule pipeline. Review the implementation plan for the SYNTAX G8 group (argument validation order).

READ the plan at /tmp/opencode/plans/SYNTAX_G8.md first.

Then verify against the codebase:
- Read crates/mlt_rules/src/syntax_errors.rs (especially the engine structure and check_file dispatch, tests mod).
- Run the AST probe in /tmp/opencode/astprobe to confirm how `x (1,1) {mustBeReal, double}` and `x (1,1) double {mustBeReal}` parse (property node children: identifier, dimensions, class identifier, validation_functions).

Adjudicate: is the order detection (class identifier byte position AFTER validation_functions → violation) correct with the real grammar? The probe showed `x (1,1) {mustBeReal, double}` produces `validation_functions "{mustBeReal, double}"` containing both names — so the class `double` inside the braces is the violation. Is identifying the class-inside-braces pattern the right trigger? Are MUST/MUST NOT cases right? file/step/test requirements complete?

Return exactly:
- `VERDICT: APPROVED` or `VERDICT: CHANGES REQUIRED`
- A short list of required corrections (or confirmations if approved)
- The corrected, complete implementation plan (repeat the whole plan with fixes) — self-contained, since the Implementer only sees your message.