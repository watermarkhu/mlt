Rule ID: FLUDLR
Category: Readability (3.3)
Module: crates/mlt_rules/src/readability.rs

You are the Implementer in mlt's rule pipeline. Implement FLUDLR exactly per the approved plan.

READ the approved plan at /tmp/opencode/plans/FLUDLR.md first. It is a sub-check of the existing READABILITY_ENGINE (no new module, no lib.rs change, no inventory::submit!).

IMPORTANT: also fix the module doc bug — readability.rs line ~28 currently mis-describes FLUDLR as "prefer fullfile". Change it to describe the flipud(fliplr(x)) → rot90(x,2) check.

Then implement:
- crates/mlt_rules/src/readability.rs: add check_fludlr (dispatched for "flipud"|"fliplr" in check_function_call), fix the module doc bullet, and all tests in the plan (fires, not-fires, config, fix).
- docs/fludlr.md (new, per docs/nosemi.md template; note sub-check + READABILITY_ENGINE disabled_checks config).
- docs/rules.md: add row.
- zensical.toml: add nav entry.

Verify:
cargo build
cargo clippy --all-targets
cargo test

Fix any failures until clean. Do NOT add code comments unless the plan requires them. Do NOT commit.

Return a summary: files created/modified, whether it fires/not-fires on the plan's examples, cargo results, deviations from the plan.