Rule IDs: NOFIL + RDERR (combined — implement BOTH as one logical change)
Category: Incomplete Analysis (3.6)
Module: crates/mlt_cli/src/main.rs

You are the Implementer in mlt's rule pipeline. Implement NOFIL and RDERR exactly per the approved combined plan.

READ the approved plan at /tmp/opencode/plans/NOFIL_RDERR.md first. These are CLI-level guards — NOT registered rules. Do NOT create nofil.rs/rderr.rs rule modules or inventory::submit! entries.

CRITICAL: First read crates/mlt_cli/src/main.rs (especially the file-read loop around lines 48-54 and the diagnostic-printing loop around lines 73-88) and adapt the plan's sketch to the actual code.

Then implement:
- crates/mlt_cli/src/main.rs: add `use std::io;` (+ `Diagnostic`, `Path` imports), replace the fs::read_to_string hard-abort with the match emitting NOFIL (NotFound) / RDERR (other errors) and continuing, add print_diagnostic helper, refactor the existing loop to use it.
- crates/mlt_cli/Cargo.toml: add [dev-dependencies] tempfile = "3".
- crates/mlt_cli/tests/nofil_rderr.rs (new): integration tests using env!("CARGO_BIN_EXE_mlt") + tempfile.
- docs/nofil.md (new) and docs/rderr.md (new) — CLI-guard docs (no target node types, no config, cannot disable).
- docs/rules.md: add both rows.
- zensical.toml: add both nav entries.

Verify:
cargo build
cargo clippy --all-targets
cargo test

Manual spot-check:
cargo run -- /tmp/definitely_missing.m  (expect NOFIL)
cargo run -- /tmp  (expect RDERR)

Fix any failures until clean. Do NOT add code comments unless the plan requires them. Do NOT commit.

Return a summary: files created/modified, cargo results, deviations from the plan.